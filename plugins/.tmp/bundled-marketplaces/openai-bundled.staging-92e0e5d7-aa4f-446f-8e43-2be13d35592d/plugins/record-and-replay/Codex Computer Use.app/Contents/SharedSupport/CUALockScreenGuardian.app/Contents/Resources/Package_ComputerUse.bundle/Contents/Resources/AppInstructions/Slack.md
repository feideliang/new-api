## Slack Computer Use

Slack enters typed text into the message composer when no text field is focused. Before pressing `Return`, make sure the intended text field is focused to avoid inadvertently sending a message. If the AX text from calling `get_app_state(...)` on Slack is behaving unexpectedly, use screenshots as the source of truth.
